{
  "name": "atomic",
  "description": "",
  "version": "0.0.3",
  "author": "nullabletypo",
  "license": "MIT",
  "source": "src/index.ts",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENCE"
  ],
  "homepage": "https://github.com/nullabletypo/atomic#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nullabletypo/atomic.git"
  },
  "bugs": {
    "url": "https://github.com/nullabletypo/atomic/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.6",
    "@typoerr/eslint-config": "^0.0.9",
    "eslint": "^5.16.0",
    "jest": "^24.7.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tsconfig-paths": "^3.8.0",
    "typescript": "^3.4.4"
  },
  "scripts": {
    "compile": "run-p compile:*",
    "compile:esm": "tsc --outDir dist/esm",
    "compile:cjs": "tsc --outDir dist/cjs --module commonjs",
    "clean": "run-p clean:**",
    "clean:dist": "rimraf dist",
    "lint": "eslint src/**/*.ts",
    "release:github": "git add . && git commit -am $npm_package_version --allow-empty && git tag $npm_package_version && git push && git push --tags",
    "release": "run-s test clean compile release:github",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "verbose": true,
    "globals": {
      "ts-jest": {
        "tsConfig": "test/tsconfig.json"
      }
    }
  }
}
